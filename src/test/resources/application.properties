# Propriedades específicas APENAS para os testes
# Isso NUNCA vai para o .jar de produção

# Garante que o TokenService não quebre a subida do contexto de testes
api.security.token.secret=chave_falsa_apenas_para_passar_nos_testes_locais_123456

# Opcionalmente, você pode configurar o banco em memória (H2) aqui caso adicione no pom.xml
# spring.datasource.url=jdbc:h2:mem:testdb
